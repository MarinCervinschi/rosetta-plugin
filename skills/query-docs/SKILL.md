---
name: query-docs
description: Not yet implemented. Phase B4 of the rosetta-plugin roadmap — will fetch /llms.txt from a running Rosetta docs server, rank pages by relevance, and return cited synthesized answers.
disable-model-invocation: true
---

# query-docs (stub)

This skill is a placeholder for **Phase B4** of the [rosetta-plugin roadmap](https://github.com/MarinCervinschi/rosetta-plugin). It will be completed in a later iteration.

When invoked today, tell the user:

> The `query-docs` skill is not yet implemented. It is scheduled for Phase B4 of the rosetta-plugin roadmap. For now, fetch `http://localhost:4321/llms.txt` manually with `curl` and read individual MDX files under `docs/src/content/docs/`.
